CREATE TABLE `lunch`.`lunch` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `salad` VARCHAR(45) NULL,
  `soup` VARCHAR(45) NULL,
  `main_dish` VARCHAR(45) NULL,
  `beverage` VARCHAR(45) NULL,
  `desert` VARCHAR(45) NULL,
  PRIMARY KEY (`id`));